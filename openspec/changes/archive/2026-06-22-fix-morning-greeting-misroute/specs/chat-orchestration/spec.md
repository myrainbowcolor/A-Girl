## ADDED Requirements

### Requirement: 早安寒暄与工作压力路由区分

开放话题拼装层（`compose_contextual_reply`）SHALL 在用户消息含早安问候（如「早呀」「早安」「早上好」）或通勤口语（如「又要上班」）且不含明显 vent 词（累、烦、加班、好烦等）时，返回口语化早安寒暄，MUST NOT 误用工作压力 vent 模板（如「忙不过来」「不公平」类追问）。

#### Scenario: 早安通勤不误路由工作压力

- **WHEN** 用户输入「早呀，今天又要上班了」且 history 为空
- **THEN** 拼装回复含早安语气（如「早呀」「开工」），不含「忙不过来」「不公平」等工作 vent 套话

#### Scenario: 真实工作倾诉仍走 vent

- **WHEN** 用户输入「今天加班到十点，好累好烦」
- **THEN** 拼装或场景回复仍体现工作压力共情，行为与变更前一致
